#!groovy
pipeline{
 agent any
 
  tools {
    maven 'maven'
  }
 
 stages{
 
   stage("build"){
   
       steps{
       
          bat "mvn -f ./rmapi clean install -DskipTests"
          bat "docker build -f rmapi/dockerfile -t rmapiapp ./rmapi"
       }
   
   }
 
 }
 
 
 
}
